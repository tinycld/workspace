diff --git a/lib/module/SortableBoardContainer.js b/lib/module/SortableBoardContainer.js
index a8499b8b6843b5a0d6108e58ca986e43dd2a40e4..c0f60ae4b9ea7e82de98d447d47d7971f4727f52 100644
--- a/lib/module/SortableBoardContainer.js
+++ b/lib/module/SortableBoardContainer.js
@@ -322,6 +322,16 @@ export const SortableBoardContainer = ({
         return snapTarget;
       }
     }
+    // A successful cross-container drop has already been dispatched when
+    // sourceInfoRef is cleared while the transfer is still pending (the
+    // transfer is consumed later by finalizeTransfer at snap end). RNGH's
+    // finalize can emit a second, cancelled drag-end after the normal one —
+    // on web the dragged id intentionally stays set through the snap, so the
+    // duplicate passes useDragGesture's guard. Reinjecting here would undo
+    // the drop; ignore the stale event instead.
+    if (cancelled && transfer?.targetId && !sourceInfoRef.current) {
+      return draxViewProps?.onMonitorDragEnd?.(eventData);
+    }
     if (transfer) {
       // Cancelled or no target — clear phantom and reinject
       if (transfer.targetId) {
diff --git a/src/SortableBoardContainer.tsx b/src/SortableBoardContainer.tsx
index 1ef725e6d18979e815a541b2289f9d9815f24661..5442a949d39eb019d485383960a23374c6d607ab 100644
--- a/src/SortableBoardContainer.tsx
+++ b/src/SortableBoardContainer.tsx
@@ -365,6 +365,17 @@ export const SortableBoardContainer = <TItem,>({
       }
     }
 
+    // A successful cross-container drop has already been dispatched when
+    // sourceInfoRef is cleared while the transfer is still pending (the
+    // transfer is consumed later by finalizeTransfer at snap end). RNGH's
+    // finalize can emit a second, cancelled drag-end after the normal one —
+    // on web the dragged id intentionally stays set through the snap, so the
+    // duplicate passes useDragGesture's guard. Reinjecting here would undo
+    // the drop; ignore the stale event instead.
+    if (cancelled && transfer?.targetId && !sourceInfoRef.current) {
+      return draxViewProps?.onMonitorDragEnd?.(eventData);
+    }
+
     if (transfer) {
       // Cancelled or no target — clear phantom and reinject
       if (transfer.targetId) {
