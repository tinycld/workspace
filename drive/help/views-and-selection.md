---
title: Views and selecting files
summary: Switching between list and grid, and selecting multiple files at once
tags: [view, list, grid, selection]
order: 120
---

## List vs Grid

The toolbar has a **List / Grid** toggle. Pick whichever fits the task:

- **List** — one row per file with name, owner, last-modified time, and size. Best for scanning recently-changed files, sorting, and large folders.
- **Grid** — thumbnail cards. Best for image and video folders, PDFs, and visual browsing.

The choice is remembered per device. Different devices can use different views.

## Sorting

In List view, click any column header to sort by that column. Click again to reverse the direction. Available columns: Name, Owner, Modified, Size.

In Grid view, sort is name-only (A → Z).

## Selecting files

- **Click** a file to select it.
- **Click another** to switch the selection.
- **Shift-click** to select a range of files between your last click and the new one.
- **Ctrl/⌘-click** (Cmd-click on Mac) to add or remove individual files from the selection — useful for grabbing a disjoint set.

On iPad, long-press a file to enter selection mode, then tap additional files to add them.

## Selecting everything

- **⌘A** (Ctrl+A on Windows) in any view selects every visible file.
- **Esc** clears the selection.

## Acting on the selection

When more than one file is selected, the toolbar shows the actions that apply to all of them — typically **Download** (as a zip), **Move**, **Share**, **Move to trash**. Actions specific to a single file (Rename, Preview, Star) are unavailable until you reduce the selection.

## See also

- [Files](help://drive:files)
- [Folders](help://drive:folders)
